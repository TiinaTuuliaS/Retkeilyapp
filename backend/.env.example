# Copy to .env only when setting up a new environment.
DB_HOST=localhost
DB_PORT=5433
DB_USER=postgres
DB_PASSWORD=
DB_NAME=retkeilyapp

# Exact frontend origin for credentialed requests and CSRF checks.
FRONTEND_ORIGIN=http://localhost:5173
# Set NODE_ENV=production for HTTPS Secure session cookies when deploying.
